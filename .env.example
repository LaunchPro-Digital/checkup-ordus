# ── Frontend (VITE_ prefix = exposed to browser) ────────────────────────────
VITE_WEBHOOK_URL=https://flow.launchpro.digital/webhook/crp-analyze
VITE_REGISTER_URL=/api/register
VITE_AGENDA_URL=https://devolutiva.ordusdigital.com.br/checkup-de-credibilidade

# ── Server-side (Vercel Functions — NEVER exposed to browser) ───────────────
# Set these in Vercel Dashboard → Project → Settings → Environment Variables
GHL_PIT_TOKEN=pit-xxxx-xxxx-xxxx
GHL_LOCATION_ID=C7Zw4DpSudSwERSPNq60
